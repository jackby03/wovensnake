name: Release & Publish

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  # --------------------------------------------------------------------------
  # 1. BUILD BINARIES & GITHUB RELEASE
  # --------------------------------------------------------------------------
  github-release:
    name: Build Release (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: wovensnake
            asset_name: wovensnake-linux-amd64
          - os: windows-latest
            artifact_name: wovensnake.exe
            asset_name: wovensnake-windows-amd64.exe
          - os: macos-latest
            artifact_name: wovensnake
            asset_name: wovensnake-macos-amd64

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - name: Build Release
        run: cargo build --release --verbose

      # Prepare asset name (Cross-platform way using bash which works on all runners)
      - name: Prepare Asset
        shell: bash
        run: |
          cd target/release
          mv ${{ matrix.artifact_name }} ${{ matrix.asset_name }}

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: target/release/${{ matrix.asset_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # --------------------------------------------------------------------------
  # 2. PUBLISH TO CRATES.IO
  # --------------------------------------------------------------------------
  crates-io-publish:
    name: Publish to Crates.io
    needs: github-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - name: Publish to Crates.io
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish
